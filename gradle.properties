org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Единственный источник версии SDK: отсюда её берут и maven-публикация, и BuildConfig
# (а из BuildConfig — MeerBot.VERSION и заголовок X-SDK-Version). Релизный скрипт и CI
# сверяют тег именно с этим значением.
SDK_VERSION=0.2.2
